node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
yarn.lock
.env
.env.local
.env.production
# 環境変数の受け渡し用に手で置いたコピー（*.incoming）も追跡しない。
# gitignore から漏れていたため `git add -A` で秘密値ごとコミットされた実績がある。
*.incoming
# .env を書き換える際のバックアップも同様に追跡しない
*.bak

# OS
.DS_Store

# IDE
.vscode/
.claude/

# Build outputs
dist/
build/

# Firebase
.firebase/
firebase-debug.log
# エミュレータ実行時に生成される（test/firestore.rules.test.mjs の手順）
firestore-debug.log
ui-debug.log

# Vercel
.vercel/

# Debug logs
pglite-debug.log
**/pglite-debug.log
build.log

# Credentials (never commit)
credentials.json
**/credentials.json
*.secret.json
*.local.json
kakeibo_service.json
vision-key.json
service-account*.json

# Google Cloud SDK
google-cloud-sdk/
google-cloud-*.tar.gz

# Base64 encoded secrets
*.b64
